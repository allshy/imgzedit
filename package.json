{
  "name": "imgzedit",
  "version": "1.0.0",
  "private": true,
  "description": "Android wrapper for the Image View multi-model image and video generation tool.",
  "scripts": {
    "prepare:web": "node scripts/prepare-web.mjs",
    "cap:sync": "npm run prepare:web && npx cap sync android",
    "android:debug": "npm run cap:sync && node scripts/build-android.mjs debug",
    "android:release": "npm run cap:sync && node scripts/build-android.mjs release"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/filesystem": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
